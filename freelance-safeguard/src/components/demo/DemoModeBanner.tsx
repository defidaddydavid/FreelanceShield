// This component has been deprecated as the application now exclusively uses real Solana blockchain connections.
// This file is kept as a placeholder to prevent import errors until all references are removed.

import React from 'react';

export const DemoModeBanner: React.FC = () => {
  // Always return null as demo mode is completely removed
  return null;
};

export default DemoModeBanner;
